@model IEnumerable<Prosjekt1.Models.Genre>
<ul class="dropdown-menu">
    @foreach (var genre in Model)
    {
        <li>@Html.ActionLink(genre.Name, "BrowseGenre", new { name = genre.Name })</li>
    }
</ul>
